package DayOne;

import java.util.Arrays;
import java.util.List;

public class Que2 {

	public static void main(String[] args) {
		List<Integer> list = Arrays.asList(1,12,21,1112,4512,34,678,3);
		System.out.println(list.stream().map(s->s+"").filter(s->s.contains("1")).toList());
		
	}
}
